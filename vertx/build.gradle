def versionVertx = '3.4.2'

dependencies {
    compile "io.vertx:vertx-core:${versionVertx}"
    compile "io.vertx:vertx-shell:${versionVertx}"
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
